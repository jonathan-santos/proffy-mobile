import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    padding: 40,
    backgroundColor: '#7257E5'
  },

  topBar: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between'
  },

  title: {
    fontFamily: 'Poppins_600SemiBold',
    color: '#FFF',
    fontSize: 24,
    lineHeight: 32,
    maxWidth: '70%',
    marginVertical: 40
  }
})

export default styles
